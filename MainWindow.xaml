<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Yoink_Downloader.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Yoink_Downloader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Yoink Downloader">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar"
                  Title="Yoink Downloader"
                  IsPaneToggleButtonVisible="True"
                  PaneToggleRequested="AppTitleBar_PaneToggleRequested"/>

        <NavigationView x:Name="NavView"
                        Grid.Row="1"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        IsPaneToggleButtonVisible="False"
                        PaneDisplayMode="LeftCompact"
                        OpenPaneLength="200"
                        SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Download" Tag="download" IsSelected="True">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Download" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Convert" Tag="convert">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Trim" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Queue" Tag="queue">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="List" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Settings" Tag="settings">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Setting" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <Grid Grid.Row="1">
            <InfoBar x:Name="LoadingBar"
                 IsOpen="True"
                 Severity="Informational"
                 Title="Loading..."
                 Message="Please wait"
                 IsClosable="True"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Bottom"
                 Margin="0,0,20,20"
                 MaxWidth="300">
                <!--ProgressRing IsActive="True" Width="20" Height="20"/-->
            </InfoBar>

            <InfoBar x:Name="LoadingBar2" IsOpen="True" IsClosable="True" IsIconVisible="False"
 HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,20,20" Message="Hello">
                <ProgressRing IsActive="True" Width="20" Height="20"/>
            </InfoBar>
        </Grid>
    </Grid>
</Window>
